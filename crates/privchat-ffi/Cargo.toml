[package]
name = "privchat_ffi"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"
tokio = { version = "1", features = ["full"] }
privchat_sdk = { path = "../privchat-sdk" }
privchat_protocol = { path = "../../../privchat-protocol" }
msgtrans = { path = "../../../msgtrans-rust" }

[lib]
crate-type = ["cdylib"]
